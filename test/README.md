# Test de fumée

```bash
cd test
npm install jsdom d3
node smoke.js
```
Vérifie : masquage avant/après tracé, couleur « auto » lue depuis la trace, lien sur le titre,
découpe du profil en portions de pente, attribut `title` de l'entête, `clear`, `collapsable`, transparence 0.
